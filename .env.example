# # [cite_start].env.example with all configurable settings [cite: 12]

# # Scraper configuration
# MOSPI_SEED_URL="https://www.mospi.gov.in/web/mospi/press-note"
# MOSPI_MAX_PAGES=5
# MOSPI_RATE_LIMIT_SECONDS=1
# USER_AGENT="Mozilla/5.0 (compatible; MoSPIScraper/1.0; +https://yourwebsite.com/)"

# # ETL configuration
# CHUNK_SIZE=1024
# CHUNK_OVERLAP=200

# # RAG configuration
# OLLAMA_MODEL="llama3"
# EMBEDDING_MODEL="sentence-transformers/all-MiniLM-L6-v2"
# VECTOR_STORE_PATH="./data/processed/vector_store"
# TOP_K=5
# TEMPERATURE=0.0

# .env.example

# Scraper configuration
MOSPI_SEED_URL="https://www.mospi.gov.in/press-release"
MOSPI_MAX_PAGES=5
MOSPI_CONCURRENCY=2 # Number of concurrent requests (optional for now, but good to plan)
MOSPI_RATE_LIMIT_SECONDS=1 # Seconds to wait between requests
LOG_LEVEL="INFO" # DEBUG, INFO, WARNING, ERROR, CRITICAL
USER_AGENT="Mozilla/5.0 (compatible; MoSPIScraper/1.0; +https://yourwebsite.com/)" # Replace with your actual website if applicable